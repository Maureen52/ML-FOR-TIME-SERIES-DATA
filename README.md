# ML-FOR-TIME-SERIES-DATA
This project forecasts electricity consumption using historical hourly energy data. The data was converted into a time-series format, relevant time features were created, and an XGBoost regression model was trained to predict future energy usage. The model’s predictions were then evaluated by comparing them with actual consumption values.
